#include<stdio.h>
#include<conio.h>
main()
{
       long int n, first, last, sum;
	clrscr();

	printf("Enter any number : ");
	scanf("%ld",&n);

	last = n % 10;

	while(n!=0)
	{
		first = n;
		n = n / 10;
	}

	sum = first + last;

	printf("\nSum of first and last digit is :-  %d",sum);
	getch();

}